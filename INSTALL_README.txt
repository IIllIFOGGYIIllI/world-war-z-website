WORLD WAR Z WEBSITE v1.40.0 - MEMBER DASHBOARD / HOME OVERHAUL

UPLOAD/REPLACE ONLY THE FILES IN THIS ARCHIVE, PRESERVING THEIR PATHS.

This patch is built directly on Website v1.39.0 and pairs with Bot v1.32.0.

What it changes:
- Rebuilds the signed-in dashboard Overview into a personalised member command centre.
- Adds at-a-glance survivor, progression, economy, faction, quest and Action Centre metrics.
- Adds current-world intelligence, priorities, events, orders/support, recent activity and quick links.
- Adds responsive/touch polish plus partial-source failure handling and active-view refresh.
- Advances the public website/PWA release label to v1.40.0 without rotating map-cache generation.

Compatibility:
- Upload Bot v1.32.0 as the paired backend release.
- No database migration, DayZ mission upload, Nitrado configuration change, map-data change or wipe is required.

After upload, commit/push the files and allow GitHub Pages/PWA update delivery to refresh normally.
