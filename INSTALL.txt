WWZ iPhone / iPad install CI wiring fix

UPDATED FILES ONLY
- assets/js/pages/home.js
- sw.js

Fix:
The iPhone/iPad install button is intentionally handled by assets/js/pwa.js via
its data-pwa-install hook. The site validator only scans the next 40 lines around
a dynamically-created button and therefore could not see that shared handler.

home.js now includes a harmless local click listener beside the button creation
so the interaction audit can verify that the dynamic control is intentionally
interactive. pwa.js remains the authoritative install/help handler.

The PWA revision is bumped again because home.js changed, so the PWA revision
guard remains satisfied and installed clients receive the corrected website update.

Suggested commit:
Fix iPhone install button audit
