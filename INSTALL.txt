WORLD WAR Z - M09 MY WWZ LIVONIA PARTIAL-DATA FIX
===================================================

UPDATED FILES ONLY
- assets/js/dashboard/my-wwz.js
- sw.js

FIX
- Stops a single optional/supplemental endpoint failure from producing the large
  yellow "1 live data source could not refresh" banner when the core Livonia
  My WWZ data is healthy.
- Core data sources still produce a visible warning if they fail:
  survivor account, progression, objectives, server status, or world intelligence.
- Optional ticket/shop/community failures now appear in the Attention card with
  an explicit "Refresh unavailable" row instead of being silently treated as zero.
- Prevents failed account/progression/shop/status requests from rendering fake
  zero/unlinked values.
- Writes exact failed source diagnostics to the browser console for debugging.
- Includes PWA revision bump.

No bot, database, Nitrado, mission or DayZ files changed.

Suggested commit:
Fix My WWZ partial data warning
