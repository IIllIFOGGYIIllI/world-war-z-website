WWZ iPhone / iPad PWA install entry point

UPDATED FILES ONLY
- assets/js/pages/home.js
- sw.js

What changes:
- iPhone/iPad visitors get an Install On iPhone / iPad button in the homepage hero instead of the Android APK download.
- The button uses the existing data-pwa-install flow. On iPhone/iPad it opens the existing Safari walkthrough: open in Safari, tap Share, Add to Home Screen, then Add.
- Android visitors keep the direct signed APK download behaviour.
- Desktop/PWA behaviour remains unchanged.
- PWA revision is bumped and cached home.js is invalidated.

Suggested commit:
Add iPhone Companion install
